{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "kbd-rtl",
  "type": "registry:example",
  "title": "Kbd RTL",
  "description": "A runnable Foldkit example showing the shadcn Kbd RTL composition.",
  "dependencies": ["effect", "foldkit"],
  "devDependencies": ["vitest"],
  "registryDependencies": ["@foldkit-cn/kbd"],
  "files": [
    {
      "path": "registry/default/examples/kbd-rtl/main.ts",
      "target": "src/examples/kbd-rtl/main.ts",
      "type": "registry:example",
      "content": "import { Schema as S } from \"effect\";\nimport type { Command } from \"foldkit\";\nimport { Submodel } from \"foldkit\";\nimport type { Html } from \"foldkit/html\";\nimport { html } from \"foldkit/html\";\n\nimport * as Kbd from \"../../ui/kbd\";\n\n// MODEL\n\nexport const Model = S.Struct({});\nexport type Model = typeof Model.Type;\n\n// MESSAGE\n\nexport const Message = S.Never;\nexport type Message = typeof Message.Type;\n\n// INIT\n\nexport const init = (): readonly [\n  Model,\n  readonly Command.Command<Message>[],\n] => [{}, []];\n\n// UPDATE\n\nexport const update = (\n  model: Model,\n  _message: Message\n): readonly [Model, readonly Command.Command<Message>[]] => [model, []];\n\n// VIEW\n\nexport const view = Submodel.defineView<Model, Message>((): Html => {\n  const h = html<Message>();\n\n  return h.div(\n    [h.Dir(\"rtl\"), h.Class(\"flex flex-wrap items-center gap-2\")],\n    [\n      h.span([h.Class(\"text-sm text-gray-700\")], [\"استخدم\"]),\n      Kbd.groupView<Message>([\n        Kbd.view<Message>({ label: \"Ctrl\" }),\n        h.span([h.Class(\"text-sm text-gray-500\")], [\"+\"]),\n        Kbd.view<Message>({ label: \"K\" }),\n      ]),\n      h.span([h.Class(\"text-sm text-gray-700\")], [\"لفتح لوحة الأوامر\"]),\n    ]\n  );\n});\n"
    },
    {
      "path": "registry/default/examples/kbd-rtl/kbd-rtl.scene.test.ts",
      "target": "src/examples/kbd-rtl/kbd-rtl.scene.test.ts",
      "type": "registry:example",
      "content": "import { Scene } from \"foldkit\";\nimport { describe, test } from \"vitest\";\n\nimport * as Example from \"./main\";\n\ndescribe(\"Kbd RTL example\", () => {\n  test(\"renders right-to-left shortcut content\", () => {\n    Scene.scene(\n      { update: Example.update, view: Example.view },\n      Scene.with(Example.init()[0]),\n      Scene.expect(Scene.text(\"استخدم\")).toExist(),\n      Scene.expect(Scene.text(\"Ctrl\")).toExist(),\n      Scene.expect(Scene.text(\"K\")).toExist(),\n      Scene.expect(Scene.text(\"لفتح لوحة الأوامر\")).toExist(),\n      Scene.expect(Scene.text(\"Ctrl\")).not.toHaveHandler(\"click\")\n    );\n  });\n});\n"
    }
  ],
  "meta": {
    "foldkit": {
      "component": "Kbd",
      "example": "rtl",
      "origin": "https://ui.shadcn.com/docs/components/radix/kbd"
    }
  }
}
