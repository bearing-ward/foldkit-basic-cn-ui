{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "label-field",
  "type": "registry:example",
  "title": "Label Field",
  "description": "A runnable Foldkit example showing the shadcn Label field composition.",
  "dependencies": ["effect", "foldkit"],
  "devDependencies": ["vitest"],
  "registryDependencies": ["@foldkit-cn/label"],
  "files": [
    {
      "path": "registry/default/examples/label-field/main.ts",
      "target": "src/examples/label-field/main.ts",
      "type": "registry:example",
      "content": "import { Schema as S } from \"effect\";\nimport type { Command } from \"foldkit\";\nimport { Submodel } from \"foldkit\";\nimport type { Html } from \"foldkit/html\";\nimport { html } from \"foldkit/html\";\n\nimport * as Label from \"../../ui/label\";\n\n// MODEL\n\nexport const Model = S.Struct({});\nexport type Model = typeof Model.Type;\n\n// MESSAGE\n\nexport const Message = S.Never;\nexport type Message = typeof Message.Type;\n\n// INIT\n\nexport const init = (): readonly [\n  Model,\n  readonly Command.Command<Message>[],\n] => [{}, []];\n\n// UPDATE\n\nexport const update = (\n  model: Model,\n  _message: Message\n): readonly [Model, readonly Command.Command<Message>[]] => [model, []];\n\n// VIEW\n\nexport const view = Submodel.defineView<Model, Message>((): Html => {\n  const h = html<Message>();\n\n  return h.div(\n    [h.Class(\"grid w-full max-w-sm items-center gap-3\")],\n    [\n      Label.view<Message>({\n        forId: \"email\",\n        children: \"Email\",\n      }),\n      h.input([\n        h.Id(\"email\"),\n        h.Type(\"email\"),\n        h.Placeholder(\"m@example.com\"),\n        h.Class(\n          \"h-9 rounded-md border border-gray-300 bg-transparent px-3 py-1 text-sm shadow-sm outline-none focus-visible:ring-2 focus-visible:ring-gray-950\"\n        ),\n      ]),\n    ]\n  );\n});\n"
    },
    {
      "path": "registry/default/examples/label-field/label-field.scene.test.ts",
      "target": "src/examples/label-field/label-field.scene.test.ts",
      "type": "registry:example",
      "content": "import { Scene } from \"foldkit\";\nimport { describe, test } from \"vitest\";\n\nimport * as Example from \"./main\";\n\ndescribe(\"Label Field example\", () => {\n  test(\"associates the label with the email field\", () => {\n    Scene.scene(\n      { update: Example.update, view: Example.view },\n      Scene.with(Example.init()[0]),\n      Scene.expect(Scene.text(\"Email\")).toExist(),\n      Scene.expect(Scene.role(\"textbox\", { name: \"Email\" })).toExist(),\n      Scene.expect(Scene.text(\"Email\")).not.toHaveHandler(\"click\")\n    );\n  });\n});\n"
    }
  ],
  "meta": {
    "foldkit": {
      "component": "Label",
      "example": "field",
      "origin": "https://ui.shadcn.com/docs/components/radix/label"
    }
  }
}
