{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "resizable-handle",
  "type": "registry:example",
  "title": "Resizable Handle",
  "description": "A runnable Foldkit example showing the shadcn Resizable Handle pattern.",
  "dependencies": ["effect", "foldkit"],
  "devDependencies": ["vitest"],
  "registryDependencies": ["@foldkit-cn/resizable"],
  "files": [
    {
      "path": "registry/shadcn/examples/resizable-handle/main.ts",
      "target": "src/examples/resizable-handle/main.ts",
      "type": "registry:example",
      "content": "import { Schema as S } from \"effect\";\nimport type { Command } from \"foldkit\";\nimport { Submodel } from \"foldkit\";\nimport type { Html } from \"foldkit/html\";\n\nimport * as Resizable from \"../../ui/resizable\";\n\n// MODEL\n\nexport const Model = S.Struct({});\nexport type Model = typeof Model.Type;\n\n// MESSAGE\n\nexport const Message = S.Never;\nexport type Message = typeof Message.Type;\n\n// INIT\n\nexport const init = (): readonly [\n  Model,\n  readonly Command.Command<Message>[],\n] => [{}, []];\n\n// UPDATE\n\nexport const update = (\n  model: Model,\n  _message: Message\n): readonly [Model, readonly Command.Command<Message>[]] => [model, []];\n\n// VIEW\n\nexport const view = Submodel.defineView<Model, Message>((): Html => {\n  return Resizable.panelGroupView<Message>({\n    className: \"min-h-48\",\n    children: [\n      Resizable.panelView<Message>({ size: 50, children: [\"One\"] }),\n      Resizable.handleView<Message>({\n        className: \"w-4 rounded-sm border border-gray-200 bg-gray-50\",\n        children: [\"||\"],\n      }),\n      Resizable.panelView<Message>({ size: 50, children: [\"Two\"] }),\n    ],\n  });\n});\n"
    },
    {
      "path": "registry/shadcn/examples/resizable-handle/resizable-handle.scene.test.ts",
      "target": "src/examples/resizable-handle/resizable-handle.scene.test.ts",
      "type": "registry:example",
      "content": "import { Scene } from \"foldkit\";\nimport { describe, test } from \"vitest\";\n\nimport * as Example from \"./main\";\n\ndescribe(\"Resizable Handle example\", () => {\n  test(\"renders a visible inert handle\", () => {\n    Scene.scene(\n      { update: Example.update, view: Example.view },\n      Scene.with(Example.init()[0]),\n      Scene.expect(Scene.text(\"One\")).toHaveAttr(\"data-size\", \"50\"),\n      Scene.expect(Scene.text(\"Two\")).toHaveAttr(\"data-size\", \"50\"),\n      Scene.expect(Scene.text(\"||\")).toExist(),\n      Scene.expect(Scene.text(\"||\")).not.toHaveHandler(\"pointerdown\")\n    );\n  });\n});\n"
    }
  ],
  "meta": {
    "foldkit": {
      "component": "Resizable",
      "example": "handle",
      "origin": "https://ui.shadcn.com/docs/components/radix/resizable"
    }
  }
}
