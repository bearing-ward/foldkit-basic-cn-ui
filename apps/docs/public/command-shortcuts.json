{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "command-shortcuts",
  "type": "registry:example",
  "title": "Command Shortcuts",
  "description": "A runnable Foldkit version of the upstream shadcn Command Shortcuts example.",
  "dependencies": ["effect", "foldkit"],
  "devDependencies": ["vitest"],
  "registryDependencies": ["@foldkit-cn/command"],
  "files": [
    {
      "path": "registry/shadcn/examples/command-shortcuts/main.ts",
      "target": "src/examples/command-shortcuts/main.ts",
      "type": "registry:example",
      "content": "import { Array, Match as M, Schema as S } from \"effect\";\nimport type { Command as FoldkitCommand } from \"foldkit\";\nimport { Submodel } from \"foldkit\";\nimport type { Html } from \"foldkit/html\";\nimport { m } from \"foldkit/message\";\nimport { evo } from \"foldkit/struct\";\n\nimport * as Command from \"../../ui/command\";\n\n// MODEL\n\nexport const Model = S.Struct({\n  query: S.String,\n});\nexport type Model = typeof Model.Type;\n\n// MESSAGE\n\nexport const UpdatedCommandQuery = m(\"UpdatedCommandQuery\", {\n  value: S.String,\n});\n\nexport const Message = S.Union([UpdatedCommandQuery]);\nexport type Message = typeof Message.Type;\n\n// INIT\n\nexport const init = (): readonly [\n  Model,\n  readonly FoldkitCommand.Command<Message>[],\n] => [{ query: \"\" }, []];\n\n// UPDATE\n\nexport const update = (\n  model: Model,\n  message: Message\n): readonly [Model, readonly FoldkitCommand.Command<Message>[]] =>\n  M.value(message).pipe(\n    M.withReturnType<readonly [Model, readonly FoldkitCommand.Command<Message>[]]>(),\n    M.tagsExhaustive({\n      UpdatedCommandQuery: ({ value }) => [\n        evo(model, { query: () => value }),\n        [],\n      ],\n    })\n  );\n\n// VIEW\n\nconst groupedItems = (\n  items: readonly Command.CommandItem[]\n): readonly [string, readonly Command.CommandItem[]][] =>\n  Array.dedupe(items.map((item) => item.group)).map((group) => [\n    group,\n    items.filter((item) => item.group === group),\n  ]);\n\nexport const view = Submodel.defineView<Model, Message>((model): Html => {\n  const items = Command.filterItems(Command.defaultItems, model.query);\n\n  return Command.rootView<Message>({\n    classes: \"max-w-md\",\n    children: [\n      Command.inputView<Message>({\n        value: model.query,\n        onInput: (value) => UpdatedCommandQuery({ value }),\n      }),\n      Command.listView<Message>({\n        children:\n          items.length === 0\n            ? [Command.emptyView<Message>({ children: [\"No results found.\"] })]\n            : groupedItems(items).flatMap(([group, groupItems], index) => [\n                ...(index === 0 ? [] : [Command.separatorView<Message>()]),\n                Command.groupView<Message>({\n                  heading: group,\n                  children: groupItems.map((item) =>\n                    Command.itemView<Message>({\n                      item,\n                    })\n                  ),\n                }),\n              ]),\n      }),\n    ],\n  });\n});\n"
    },
    {
      "path": "registry/shadcn/examples/command-shortcuts/command-shortcuts.scene.test.ts",
      "target": "src/examples/command-shortcuts/command-shortcuts.scene.test.ts",
      "type": "registry:example",
      "content": "import { Scene } from \"foldkit\";\nimport { describe, test } from \"vitest\";\n\nimport * as Example from \"./main\";\n\ndescribe(\"Command Shortcuts example\", () => {\n  test(\"renders shortcut rows from the origin command example\", () => {\n    Scene.scene(\n      { update: Example.update, view: Example.view },\n      Scene.with(Example.init()[0]),\n      Scene.expect(Scene.text(\"Profile\")).toExist(),\n      Scene.expect(Scene.text(\"Billing\")).toExist(),\n      Scene.expect(Scene.text(\"Settings\")).toExist(),\n      Scene.expect(Scene.text(\"⌘P\")).toExist(),\n      Scene.expect(Scene.text(\"⌘B\")).toExist(),\n      Scene.expect(Scene.text(\"⌘S\")).toExist(),\n      Scene.expect(Scene.text(\"Profile\")).not.toHaveHandler(\"click\")\n    );\n  });\n\n  test(\"filters shortcut command content\", () => {\n    Scene.scene(\n      { update: Example.update, view: Example.view },\n      Scene.with(Example.init()[0]),\n      Scene.type(Scene.role(\"combobox\", { name: \"Command search\" }), \"bill\"),\n      Scene.expect(Scene.text(\"Billing\")).toExist(),\n      Scene.expect(Scene.text(\"⌘B\")).toExist(),\n      Scene.expect(Scene.role(\"option\", { name: \"Profile\" })).toBeAbsent()\n    );\n  });\n\n  test(\"renders empty shortcut command results\", () => {\n    Scene.scene(\n      { update: Example.update, view: Example.view },\n      Scene.with(Example.init()[0]),\n      Scene.type(Scene.role(\"combobox\", { name: \"Command search\" }), \"zzz\"),\n      Scene.expect(Scene.text(\"No results found.\")).toExist()\n    );\n  });\n});\n"
    }
  ],
  "meta": {
    "foldkit": {
      "component": "Command",
      "example": "shortcuts",
      "originExample": "command-shortcuts",
      "origin": "https://ui.shadcn.com/docs/components/radix/command"
    }
  }
}
